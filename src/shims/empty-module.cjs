/*
 * Empty CommonJS shim module.
 *
 * Used to satisfy optional/dev-only dependencies that should never be executed
 * in production bundles (e.g. dependencies referenced from vendor test files).
 */
module.exports = {};


